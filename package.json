{
  "name": "newapi-monitor-plugin",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Multi-site New API balance and group ratio monitor for TRSS-Yunzai",
  "scripts": {
    "test": "node --test test/*.test.js"
  }
}
